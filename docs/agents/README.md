# Agent guide

Treat this repository as the only capsule owner. Package roles are closed:
`sim-pure`, `platform-bootstrap`, `platform-capsule`, and `host-tool`; missing
metadata means `sim-pure`. Do not infer host authority from ownership.

