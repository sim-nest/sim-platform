# Human guide

Start with the repository README, then the `sim-platform-core` recipe. A
capsule declares a provider, services, and shells; a bundle selects exactly one
capsule artifact by content identity and the existing native ABI entrypoint.

