# sim-platform-ubuntu-rpi

The ordinary headless Ubuntu Raspberry Pi capsule. Board revision, device
paths, permissions, and absence are profile data. Domain consumers see only
their existing service contracts and typed `Unsupported` refusals.

Current evidence is cross-built and model-conformant. It is not physical: the
private test-host registry contains no Raspberry Pi host.
