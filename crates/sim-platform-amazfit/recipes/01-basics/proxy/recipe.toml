id = "amazfit-bounded-proxy"
title = "Exercise the modeled Amazfit proxy"
description = "Checks consent, session, bounded queue, and glance command behavior without hardware."
entry = "cargo test -p sim-platform-amazfit"
expected = "all proxy conformance tests pass"
tags = ["amazfit", "watch", "proxy", "modeled", "offline"]
