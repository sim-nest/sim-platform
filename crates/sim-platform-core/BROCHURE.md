# sim-platform-core

Parse and validate the closed package-role model and versioned capsule/bundle
contracts. Validation uses discovered repository and dependency context, so a
manifest cannot grant itself host authority.

