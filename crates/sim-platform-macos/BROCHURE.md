# macOS capsule

One explicit macOS provider composes filesystem, process, loading, transport,
desktop, permission, audio, MIDI, and compute ports behind SIM's stable native
ABI. It produces reproducible unsigned development bundles for Intel and Apple
Silicon while keeping every Apple framework detail out of portable crates.
