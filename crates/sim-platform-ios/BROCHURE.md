# sim-platform-ios

A thin iOS scene shell hosts the same SIM platform Card and native byte ABI as
desktop and Android capsules, with explicit security-scoped document lifetime,
closed service refusals, and no hidden background worker.
