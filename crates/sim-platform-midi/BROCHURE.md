# Native MIDI, without ambient host access

`sim-platform-midi` realizes ALSA/RtMidi and BlueZ BLE-MIDI endpoints behind the
portable music port, with typed denial, loss, backpressure, reconnect, and cleanup.
