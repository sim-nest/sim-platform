# Browser capsule

Run the same capability-honest SIM platform model in windows and workers. The
browser capsule offers only detected storage, transport, clipboard,
notification, permission, and wake-lock handles while `sim-web` retains every
presentation decision.
