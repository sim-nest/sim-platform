# Offline browser capsule

This recipe claims only the retained, modeled support recorded by
`attestations/browser-offline.json`: lifecycle and IndexedDB-style storage
handles. Network, notifications, clipboard, and wake lock remain absent.
