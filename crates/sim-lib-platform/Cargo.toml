[package]
name = "sim-lib-platform"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure loadable-facing platform manifest API"
publish = false

[dependencies]
sim-kernel = "0.2.0"
sim-platform-core = { path = "../sim-platform-core" }

[dev-dependencies]
sim-kernel = { version = "0.2.0", features = ["test-support"] }

[lints]
workspace = true
