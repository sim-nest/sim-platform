The exact same expression is used by the Ubuntu PC capsule. A headless or
otherwise incomplete card produces the typed `Unsupported` refusal for a
required service, while optional GPIO selects only its declared substitute.
