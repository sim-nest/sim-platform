# Native web shell capsule

This capsule is the Linux process rind for the host-neutral `sim-web-shell`
library. It realizes socket and DNS transport, explicit mounted-file reads,
monotonic time, entropy, and external URL opening. Reversible Surface, Scene,
Intent, session, and layout behavior remains replaceable in `sim-web`.
